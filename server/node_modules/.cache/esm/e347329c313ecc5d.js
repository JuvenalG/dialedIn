let express,readdirSync,cors,mongoose;_313‍.w("express",[["default",["express"],function(v){express=v}]]);_313‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_313‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_313‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);
 //allows whole routes folder to import -- Node.JS module -- FILESYSTEM

 //db connection object
require("dotenv").config();
const morgan = require("morgan"); //shows CRUD request in console

const app = express();

//DB connection
mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
  })
  .then(() => console.log("DB connected"))
  .catch((err) => _313‍.g.console.log("DB Error => ", err));

//middlewares
app.use(cors()); //allows cross-origin resource sharing -- also needed for express.json
app.use(morgan("dev"));
app.use(express.json()); //allows req.body etc api calls to recieve json forrmat alternative=body-parser
//route middleware
readdirSync("./routes/").map((r) => app.use("/api", require(`./routes/${r}`)));

const port = process.env.PORT || 8000;
app.listen(port, () => console.log(`Server is running on ${port}`));
